<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.outpost.account.configuration.repository.mybatis.MerchantFeeConfigurationMapper">
  <select id="hasFeeConfiguration" resultType="boolean">
    SELECT EXISTS (
      SELECT 1
        FROM merchant_fee_configuration fee
        JOIN currency ON currency.currency_id = fee.currency_id
       WHERE fee.account_id = #{merchantAccountId}
         AND currency.currency_code = #{currencyCode}
    )
  </select>
</mapper>
